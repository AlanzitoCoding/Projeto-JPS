// Louvado seja o Senhor

import { TipoCompraPipe } from './tipo-compra.pipe';

describe('TipoCompraPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoCompraPipe();
    expect(pipe).toBeTruthy();
  });
});
